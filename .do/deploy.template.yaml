spec:
  name: simple_ha_datasource
  services:
  - environment_slug: python
    git:
      branch: main
      repo_clone_url: https://github.com/gravypower/simple_ha_datasource.git
    name: simple_ha_datasource
